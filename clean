#!/bin/bash

# Author: Jaap Versteegh <j.r.versteegh@gmail.com>

curdir=`dirname $BASH_SOURCE`
curdir=`readlink -f $curdir`
cd $curdir

if [ "X$1" == "Xdist" ]; then
  rm -rf root
fi

rm -rf .stageroot
rm -rf .build
rm -f build-1.log
rm -f build-2.log

src/clean
